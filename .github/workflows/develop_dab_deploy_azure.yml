trigger:
  branches:
    include:
      - main

pool:
  vmImage: 'ubuntu-latest'

variables:
  - group: databricks-secrets

stages:
  - stage: Deploy
    displayName: Deploy to Databricks
    jobs:
      - job: DeployBundle
        displayName: Deploy Databricks Asset Bundle
        steps:

          # Checkout code
          - checkout: self

          # Install Databricks CLI
          - script: |
              curl -fsSL https://raw.githubusercontent.com/databricks/setup-cli/main/install.sh | sh
              databricks version
            displayName: Install Databricks CLI

          # # Azure Service Principal Login
          # - script: |
          #     echo "Logging into Azure..."
          #     az login --service-principal \
          #       --username $(AZURE_CLIENT_ID) \
          #       --password $(AZURE_CLIENT_SECRET) \
          #       --tenant $(AZURE_TENANT_ID)
          #   displayName: Azure Login

          # # Configure Databricks authentication (AAD)
          # - script: |
          #     mkdir -p ~/.databricks
          #     echo "[DEFAULT]" > ~/.databricks/config
          #     echo "host = $(DATABRICKS_HOST)" >> ~/.databricks/config
          #     echo "azure_client_id = $(AZURE_CLIENT_ID)" >> ~/.databricks/config
          #     echo "azure_client_secret = $(AZURE_CLIENT_SECRET)" >> ~/.databricks/config
          #     echo "azure_tenant_id = $(AZURE_TENANT_ID)" >> ~/.databricks/config
          #   displayName: Configure Databricks CLI (AAD)

          # # Validate bundle
          # - script: |
          #     databricks bundle validate
          #   displayName: Validate Bundle

          # # Deploy bundle
          # - script: |
          #     databricks bundle deploy
          #   displayName: Deploy Bundle
